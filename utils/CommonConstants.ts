// This constant is used to define the number of items to be displayed per page in paginated lists.
export const PAGE_LIMIT = 12;

// This constant is used to define the debounce time for search input fields.
export const SEARCH_DEBOUNCE_MS = 400;
